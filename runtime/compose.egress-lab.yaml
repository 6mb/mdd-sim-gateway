# Development-only country proxy service. This does not implement full hardware
# deployment yet. No host ports, host networking, TUN device or NET_ADMIN.
name: mdd-egress-lab
services:
  egress:
    image: mdd-sim-gateway/egress:dev
    build:
      context: ..
      dockerfile: runtime/Dockerfile.egress
      args:
        TARGETARCH: ${MDD_ARCH:-amd64}
    restart: unless-stopped
    read_only: true
    cap_drop: [ALL]
    security_opt: [no-new-privileges:true]
    pids_limit: 64
    mem_limit: 256m
    tmpfs: [/tmp:size=16m]
    volumes:
      - ${MDD_LAB_DATA:?set an absolute, dedicated lab data directory}:/data
    environment:
      MDD_EGRESS_HOST: mdd-egress
    networks:
      control:
        aliases: [mdd-egress]
      uplink: {}
    labels:
      com.centurylinklabs.watchtower.enable: "false"
    logging:
      driver: json-file
      options:
        max-size: 10m
        max-file: "3"
networks:
  control:
    internal: true
  uplink: {}
